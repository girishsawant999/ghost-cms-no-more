{{!< default}}
{{> "header"}}

  {{!-- nf about section --}}
  <section class="nf-about-section pb-5">
    {{> "nf-page-title"}}

    <div class="container pt-3">
      <div class="row">
        <div class="col-md-8">
          {{#post}}
            <div class="entry-content my-1">
                {{content}}
            </div>
          {{/post}}
        </div>

        <div class="col-lg-4 col-md-5">
          {{> "sidebar"}}
        </div>
      </div>
    </div>
  </section>
  {{!-- nf about section --}}

{{> "cta-section"}}
  
{{> "footer"}}