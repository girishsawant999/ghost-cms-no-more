{{!-- version 2.5.1 --}}

<header class="header-area">
   {{!--
   <div class="header-navbar-top py-1 bg-light border-bottom border-light  d-none d-lg-block">
      <div class="container">
         
         <div class="header-newsticker-menu">
            
            <div class="newsticker">
               <div class="newsticker-btn">{{t "Latest News"}}</div>
               <div class="newsticker-list">
                  <ul class="list-inline">
                     {{#get "posts" limit="5"}}
                     {{#foreach posts }}
                     <li> <a href="{{url}}">{{title}}</a> </li>
                     {{/foreach}}
                     {{/get}}
                  </ul>
               </div>
            </div>
            
            <div class="newsticker-social">
               <ul class="list-inline">
                  {{#if @site.facebook}}
                  <li class="list-inline-item"><a class="bg-primary" href="{{facebook_url}}" aria-label="Facebook" ><i class="fab fa-facebook-f"></i></a>
                  </li>
                  {{/if}}
                  {{#if @site.twitter}}
                  <li class="list-inline-item"><a class="bg-info" href="{{twitter_url}}" aria-label="Twitter"><i class="fab fa-twitter"></i></a>
                  </li>
                  {{/if}}
                 
               </ul>
            </div>
         </div>

      </div>
   </div>
   --}}
   {{!-- 
   <div class="header-navbar-middle d-none d-lg-block">
      <div class="container">
         <div class="navbar-brand-wrapper py-3 d-flex justify-content-between align-items-center">
            <a class="navbar-brand" href="{{@site.url}}">
            {{#if @site.logo}}
            <img src="{{@site.logo}}" alt="{{@site.title}}">
            {{/if}}
            {{@site.title}}
            </a>         
         </div>
      </div>
   </div>
   --}}
   <div class="header-navbar-bottom pt-2 pt-lg-0 pb-2 pb-lg-0 border-top border-light">
      <div class="container nf-menu-selector">
         <nav class="navbar navbar-expand-lg">
            <a class="navbar-brand" href="{{@site.url}}" >
               {{#if @site.logo}}
               <img style="max-width: 120px;" src="{{@site.logo}}" alt="{{@site.title}}">
               {{/if}}
            </a>
            <div class="nf-nav-header-part">
               {{!-- 
               {{#if @site.logo}}
               <a class="navbar-brand" href="{{@site.url}}"><img src="{{@site.logo}}" alt="{{@site.title}}"></a>
               {{else}}
               <a class="navbar-brand" href="{{@site.url}}"> {{@site.title}}</a>
               {{/if}}
               --}}
               <button id="nf-navbar-toggler" type="button" aria-label="Search">
               <span class="navbar-toggler-icon"></span>
               </button>
            </div>
            <div class="main_navbar" id="main_nav">
               {{navigation}}
            </div>
            <div class="search-modal" data-bs-toggle="modal" data-bs-target="#headermodal"><a class="nav-link"
               href="#" aria-label="Search"> <i class="fas fa-search"></i> </a>
            </div>
         </nav>

{{> "search-modal"}}


      </div>
   </div>
</header>
